<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.osu">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Osu"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".Activitys.Cadastro"></activity>
        <activity android:name=".Activitys.Login" />
        <activity android:name=".Activitys.DetalhePost" />
        <activity android:name=".Activitys.ActivityPost" />
        <activity android:name=".Activitys.HistoricoScore" />
        <activity android:name=".Activitys.ActivityScore" />

        <receiver android:name=".OsuWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/osu_widget_info" />
        </receiver>

        <activity android:name=".Activitys.Localizacao" />
        <activity android:name=".Activitys.HistoricoUsuario" />
        <activity android:name=".Activitys.OsuUsuario" />
        <activity android:name=".Activitys.MainActivity" />
        <activity
            android:name=".Activitys.splash_screen"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>